# from django.urls import path
# from .views import VideoListView, HLSIndexView, HLSContentView

# urlpatterns = [
#     path('video/', VideoListView.as_view(), name='video_list'),
#     path('video/<int:movie_id>/<str:resolution>/index.m3u8', HLSIndexView.as_view(), name='hls_index'),
#     path('video/<int:movie_id>/<str:resolution>/<str:segment>/', HLSChunkView.as_view(), name='hls_chunk'),
# ]

